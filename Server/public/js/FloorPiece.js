// the floor object should have texture, shape, size, thickness

export class FloorPiece {
    constructor (color, size, thickness) {
        this.color = color;
        this.size = size;
        this.thickness = thickness;
    }
}